# Application Settings
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# MCP Server Settings
# Transport mode: 'stdio' (for Claude Desktop, Cursor) or 'streamable-http' (for HTTP access)
TRANSPORT=stdio
# HTTP server settings (only used when TRANSPORT=streamable-http)
HOST=0.0.0.0
PORT=8000

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here
REDIS_DB=0

# Memory Store Configuration
# Comma-separated list of allowed namespaces (empty = all allowed)
# Example: ALLOWED_NAMESPACES=programming-style,writing-style,preferences
ALLOWED_NAMESPACES=

# Comma-separated list of read-only files in format "namespace/key"
# Example: READ_ONLY_FILES=preferences/core-values,programming-style/company-standards
READ_ONLY_FILES=

